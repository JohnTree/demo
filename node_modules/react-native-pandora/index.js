import {NativeModules} from "react-native";

const RNPandora = {
    get Ajax() {
        return NativeModules.Ajax;
    },
    get LocalPersistence() {
        return NativeModules.LocalPersistence
    },
    get Serializer() {
        return NativeModules.Serializer
    },
    get ScanCode() {
        return NativeModules.ScanCode
    },
    get HardwareDrivers() {
        return NativeModules.HardwareDrivers
    },
    get Phone() {
        return NativeModules.Phone
    }
};
const RNPandoraInternal = RNPandora;
function applyForwarding(key) {
    if (__DEV__) {
        Object.defineProperty(
            RNPandora,
            key,
            Object.getOwnPropertyDescriptor(RNPandoraInternal, key)
        );
        return;
    }
    RNPandora[key] = RNPandoraInternal[key];
}
for (const key in RNPandoraInternal) {
    applyForwarding(key);
}
export default RNPandora;
